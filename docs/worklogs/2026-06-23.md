# 작업로그 2026-06-23 (코드리뷰·리메디에이션 사이클)

6-22 저녁~심야 연속 세션. **전수 코드리뷰 2회 + 보완 6차례 배포 + 거대파일 10개 분할.** 마이그레이션 0, 무중단, 전부 운영 검증.

## 리뷰 (2 렌즈)
- **보안/버그 리뷰** → 종합 **B−**. `docs/reviews/CODE_REVIEW_FULL_2026-06-22.md`. 발견 94건(high20·med43·low31).
- **품질/아키텍처 리뷰**(다른 렌즈) → 종합 **B−** ("설계는 옳은데 마감이 빚"). `docs/reviews/CODE_QUALITY_REVIEW_2026-06-22.md`. 발견 95건. 차원: arch 6.7(강)·consistency/modularity 5.5(약).
- **커버리지 패스** → "통째로 빠진 영역 없음" 확정(범위 완전). 단 모듈 글롭 밖 `pages/` 루트 7파일 등 덜 본 곳서 **21건 새로 발견**(깊이 갭 입증).

## 보완 배포 (순서대로)
1. **PR#59 보안 1차** — 레이트리밋(로그인/LLM/생성)·pool_pre_ping·권한재동기화·입력검증·토큰무효화·E1매칭불변식·D2 신사업접근보존·pptx가드·주차floor·React버그.
2. **PR#61 + Q3 (P0+P2)** — DRY유틸(blob다운로드7·포맷터·errorUtils)·거래해시 3→1(HASH_IDENTICAL)·주차식·404헬퍼·권한 진실원천 단일화(운영DB grant커버 확인, 락아웃0)·하드코딩 제거.
3. **P1 거대파일 10개 분할**(1·2차) — sns.py 2173·playground.py 1412·forms.py 1350·session_service.py 997 → 패키지(라우트/공개API 보존) + Transactions/SessionDetail/ProductsPage/Marketing1/Analytics/MediaGen → 서브컴포넌트. OpenAPI 188경로 보존.
4. **커버리지 20건** — ProtectedRoute role가드·/users admin게이트·streamChat 입력잠김·첨부clear·TaxInvoices 자동조회·reconcile 후보집합·balance N+1·attachments uuid.
5. **P3 일관성** — 라우터 경로파라미터 15개 uuid화·영어에러 한국어·데드코드 제거(file_walker 191줄 등)·LLM call_claude 일원화·models +4·App.tsx 라우트 데이터주도.

## 더미코드 스캔 → 깨끗
console.log 0·주석코드 0·스텁 0. print는 전부 CLI스크립트. "준비 중"은 의도된 단계출시 UX. → 정리할 정크 없음.

## 운영 교훈 (CLAUDE.md 반영)
- **FE 검증은 `npm run build`(tsc -b)** — `tsc --noEmit`는 noUnusedLocals 못 잡음(P1 1차 배포 1회 실패 원인).
- **cherry-pick은 단일 커밋형** — 범위형 `main..branch`가 파일→디렉토리 전환에서 멈춤.
- **에이전트 컨테이너 테스트가 `/srv`(메인 워킹트리) 오염 가능** — 통합 전 `git status` 확인.

## 잔류 (대형 — 별도 전용 작업/논의)
- ⚠️ **TanStack Query 도입**(서버상태 전면 교체) · **API `res.data` 언랩 컨벤션**(전 호출부) · **localStorage JWT→httpOnly 쿠키**(인증 저장모델, 로그인 위험).
- 기존 제품: SNS 수동분류·제작주체·릴스캠페인·보고서양식, 보안조치(오너승인) 등 → BACKLOG.

**상태: 핵심 기술부채 정리 완료. 코드베이스 단단해짐.**

---

# 작업로그 2026-06-23 (낮 세션 — NAS검색·문서작업·제품기능)

오너 골칫거리 2개(NAS 검색 품질, 문서작업 품질/UX) 집중 + 제품기능 4건. **기능/수정 PR 15건 + 백로그/문서 정리 다수, 전부 운영 검증.** 독립 작업은 병렬 멀티에이전트(워크트리), 통합·배포는 순차.

## A. NAS 검색 품질
- **PR#75 게이트 리랭크後 이동 + 풀 12→20.** 라이브 진단: 임베딩 대칭성은 정상인데, 정답이 raw cosine 0.55~0.72에 뭉쳐 게이트(0.50, 리랭크 前)에 잘림. 게이트를 **리랭크 점수(0.05)로 이동** → BEFORE 놓친 0.93~0.98 정답 상위노출, noise 0건. before/after 실측.
- **PR#92 풀텍스트 인덱스(P2).** 키워드(정확검색) arm이 인덱스 없어 임의 prefix 4000개(코퍼스 0.6%)만 scroll → 품번·고유명사·파일명을 거의 못 잡음. Qdrant `text`/`path` 풀텍스트 인덱스(word, 160만청크 ~1~2분 빌드, 무중단) + keyword_arm MatchText 전환. **실측: '견적서' 전체매칭 30,123 vs 과거 앞4000서 100건(99.7% 놓침)** / 계약서 20,765 vs 51 / 세금계산서 2,938 vs 5. 임베딩(의미)과 RRF 병용(대체 아님).

## B. 문서작업 품질·UX (3리뷰 → 3 Phase, 8 PR)
3 병렬 리뷰(docgen품질·양식품질·UX) → 단계별:
- **Phase1(PR#77)** temperature=0(추출/judge)·docgen max_tokens 8192→16000(502방지)·**양식 grounding 날짜/숫자 정규화**(ISO날짜가 원문과 매칭실패해 맞는값을 블랭크로 버리던 버그).
- **Phase2** bridge RAG에 bge리랭커+게이트(#78, docgen근거+검색 2-for-1) · **양식 좌표앵커 키 계약(#80)** = "표 칸 안 채워짐" 근본해결(analyzer가 `t{t}r{r}c{c}`·`p{i}` 앵커 발행→renderer 좌표 치환), +타입정규화·default폴백 · docgen 섹션 스켈레톤+judge→재생성 루프(#79).
- **Phase3 UX** 에러원인표시·진행률·필터·텔레메트리 admin게이트(#82) · 위저드 Steps 진행바(#84) · **실제 docx 미리보기 mammoth(#86)** · 작성중 문서 resume + 확정라벨(#88). 보류=양식 단일컴포넌트 병합(미관·회귀위험).

## C. 제품 기능
- **SNS 구분 필드(#91)** 행사/기획/정책/이벤트/기타, 마이그 035(`social_posts.category`), 인라인 Select+필터+집계.
- **제작주체 집계(#98)** producer가 free-text라 신규 `GET /api/sns/posts/producer-stats`(GROUP BY 동적)로 집계. 덤: 깨진 PRODUCER_LABELS 교정.
- **docwork PR-C(#97)** extractor→`services/documents/` 이전(동작보존, judge/파서/렌더는 실제로 달라 미합침).
- **포매터/린터 셋업(#101)** prettier + ruff·black 설정·의존성(전체 재포맷 없이).

## D. 정리·분석
- BACKLOG 여러 차례 갱신 + **전면 재편**(완료분 DONE 이동, NOW엔 남은 일만).
- 핸드오프 문서 `docs/HANDOFF_2026-06-23.md` 작성(다른 에이전트 콜드 인수용).
- RAG 구조 정리(검색=CPU 인코더, LLM 불필요; 생성만 외부 LLM) · 풀텍스트 인덱스 vs 임베딩 차이(병용) 설명.

## E. 운영 교훈 (추가)
- **임베딩/리랭커 측정은 운영 컨테이너(웜 모델)에서** — dev 컨테이너는 매 exec마다 8GB 콜드로드로 hang/D-state 좀비.
- **워크트리 격리 에이전트는 npm build/pytest/alembic 불가** — 통합은 메인트리에서. 병렬 에이전트가 BACKLOG 동시수정 시 머지충돌 → `git merge main` 후 `--theirs docs/BACKLOG.md`.
- **Qdrant 풀텍스트 인덱스는 재임베딩 없이 추가 가능**(이미 적재된 payload에 인덱스만).

## F. 결정/보류
- ⚠️ **TanStack / API res.data 컨벤션 / JWT→쿠키** = 오너가 "보류" 선택(앱 장애 위험).
- **엑셀 내보내기 B** = 항목 전제 오류(엑셀export는 SNS, NAS는 export 없음) → 실제 니즈 확정 후.
- 재임베딩(H4/C2/H3)·보안·디스크·임베딩스케줄 = ⚠️비용/오너승인 → BACKLOG.

**상태: NAS검색·문서작업 두 골칫거리 해결·배포. 남은 건 전부 오너 결정 필요. 마이그 head=035.**
