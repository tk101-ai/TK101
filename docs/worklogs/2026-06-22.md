# 작업로그 2026-06-22

문서작업 통합 · 마케팅 SNS 대시보드 · 문서생성 버그픽스 · docs 정리 · 보안/도메인 검토. 멀티에이전트 병렬 구현 → 순차 통합·배포(단일 러너 레이스 회피).

## 배포된 PR (전부 main 머지 + 운영 배포 완료)

| PR | 분류 | 내용 |
|---|---|---|
| #31 | docgen | 디자인 테마 + 구조 렌더(불릿/표/차트) |
| #32 | infra | 레거시 pgvector(nas_text_chunks/e5) 완전 제거(032 마이그레이션). 검색=Qdrant 단일소스 |
| #33 | refactor | 공유 LLM/RAG 인프라 중립화(llm_client→services/llm, nas_bridge→services/nas_search) + 통합 PRD |
| #34 | docgen | 출처 모드(RAG/업로드/둘다) + 사용자 참고문서 업로드 |
| #35 | nav | 메뉴 "문서 작업" 그룹 통합 |
| #37 | fix | **문서 생성 504 버그픽스** — nginx /api/ 타임아웃 60→300s |
| #38 | docs | docs/ 폴더 카테고리 구조 통합(prd/reviews/ops/worklogs/cost/decisions/assets) |
| #39 | sns | 계정 삭제 + 추이 엔드포인트 + 공유 AccountSelector (대시보드 1단계) |
| #40 | docgen | 섹션 재생성·검수에도 업로드/출처모드 반영(멀티파트 통일) |
| #41 | sns | 전체 갱신 버튼 + 통합 대시보드 (2단계) |
| #42 | documents | 생성물 NAS 저장 `/mnt/nas-rw/문서작업/{부서}/{날짜}/`(베스트에포트) |
| #43 | documents | docgen 잡 영속화 + 관리자 토큰/비용 패널(033 마이그레이션) + 일반사용자 비용숨김 |

## 핵심 진단/결정
- **문서생성 실패 원인**: 생성 로직은 정상(직접 호출로 신세계 제안서 8섹션·RAG 8건 검색 확인). **nginx /api/ 기본 60초 타임아웃**이 임베더 콜드로딩+다섹션 생성을 504로 끊은 것 → 300초로 상향.
- **DB 마이그레이션 033**: dev DB(tk101_dev)에서 적용·롤백·재적용 전수 검증 후 운영 배포. status enum 변경 없음(R-ENUM 회피), kind server_default='fill' 원자적 backfill.
- **마케팅 SNS 발견**: 플랫폼은 이미 데이터/API 계층에서 통합돼 있었음("3계정"도 DB행). 흩어진 건 프론트뿐 → 공유 셀렉터·통합 대시보드·계정삭제·전체갱신만 보강.

## 검토 문서 산출 (docs/)
- `docs/prd/PRD_DOCWORK_UNIFY_2026-06-22.md` — 문서작업 통합 설계
- `docs/prd/SPEC_DOCWORK_PRB_PRE_2026-06-22.md` — #1/#2 구현 스펙
- `docs/prd/DESIGN_UNIFIED_SNS_DASHBOARD.md` — SNS 통합 설계(6단계)
- `docs/reviews/REVIEW_SECURITY_DOMAIN_CLEANUP_2026-06-22.md` — 보안/도메인/디스크정리
- `docs/reviews/REVIEW_INCREMENTAL_EMBEDDING_FRIDAY_SCHEDULE.md` — 금요일 임베딩 파이프라인

## 미완 / 결정 대기
- **#6 임베딩(금요일 18시 RunPod)**: 구현 보류. 결정 필요 = RunPod(외부)→Qdrant(127.0.0.1) 접근방식(푸시 모델 권장). 인프라/보안 결정.
- **보안**: 🔴open-webui :3000 공개+가입ON, 🟠SSH 비번/root, backend-dev 8001 바인딩 → 미적용(콘솔/SSH 작업, 오너 결정). WireGuard·Cloudflare Tunnel 권장(문서 참조).
- **디스크 정리**: docker prune ~50GB 등 SAFE 후보 미실행(오너 승인 대기).
- **SNS 3단계**(설정형 위젯/저장된 뷰), **docwork PR-C**(검수·렌더 공유화) — 후속.

## 사용자 검증 필요
1. 문서 생성 재시도(504 해결 확인)
2. 마케팅 대시보드(전체 갱신/통합 요약/추이 차트), SNS 계정 추가·삭제
3. 관리자 토큰/비용 패널(관리자 계정으로 /documents/usage), 일반 사용자엔 비용 안 보임
4. 문서 생성/렌더 후 NAS `문서작업/` 폴더에 저장됐는지
