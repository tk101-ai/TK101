# 작업로그 2026-06-22

대규모 세션 — **PR #31~#56 (26건) 머지·운영 배포 완료.** 멀티에이전트 병렬 구현 → 순차 통합·배포(단일 러너 레이스 회피), 각 단계 운영 검증. 마이그레이션 032·033·034 dev 검증 후 적용. 운영 DB 리비전 = **034**.

## A. 문서 작업(docgen/문서생성) 통합 — "문서 작업"
| PR | 내용 |
|---|---|
| #31 | docgen 디자인 테마 + 구조 블록 렌더(불릿/표/차트) |
| #33 | 공유 LLM/RAG 인프라 중립화(`form_filler.llm_client→services/llm/client`, `nas_bridge→services/nas_search/bridge`) + 통합 PRD |
| #34 | 출처 모드(RAG/업로드/둘다) + 사용자 참고문서 업로드 |
| #35 | 네비 "문서 작업" 그룹 통합 |
| #37 | **문서 생성 504 버그픽스** — nginx `/api/` 타임아웃 60→300s (생성로직은 정상, 임베더 콜드로딩+다섹션 생성이 60초 초과) |
| #40 | 섹션 재생성·검수에도 업로드/출처모드 반영(멀티파트 통일) |
| #42 | 생성물 NAS 저장 `/mnt/nas-rw/문서작업/{부서}/{날짜}/` |
| #43 | docgen 잡 영속화 + 관리자 토큰/비용 패널(033) + 일반사용자 비용숨김 |

설계: `docs/prd/PRD_DOCWORK_UNIFY_2026-06-22.md`, `docs/prd/SPEC_DOCWORK_PRB_PRE_2026-06-22.md`.

## B. 인프라 / 문서 정리
| PR | 내용 |
|---|---|
| #32 | 레거시 pgvector(`nas_text_chunks`/e5) 완전 제거(032). 검색=Qdrant 단일소스 |
| #38 | `docs/` 폴더 카테고리 구조 통합(prd/reviews/ops/worklogs/cost/decisions/assets) |

## C. 마케팅 SNS — 통합 대시보드 + DB 관리 (구글 시트 수준)
| PR | 내용 |
|---|---|
| #39 | 계정 삭제 + 추이 엔드포인트 + 공유 AccountSelector (대시보드 1단계) |
| #41 | 전체 갱신 버튼 + 통합 대시보드 (2단계) |
| #44 | 대시보드 위젯별 독립 fetch(allSettled) — 한 API 실패가 전체 비우던 것 |
| #45 | 마케팅1팀에 "대시보드" 직접 진입 메뉴 추가(원래 없었음) |
| #46 | **전체갱신 500 버그픽스** — 미연동 placeholder 계정 수집 시 MissingGreenlet → 연동(external_id) 계정만 갱신 |
| #47 | 댓글 AI요약 영속화 + 번역 재열람 표시 (재요약/재번역 방지, 토큰절약) |
| #48 | SeoulSns 통합(기간+다중계정) + **무료수집 전직원 개방**(require_admin 제거 4개) + SnsPosts 은퇴 |
| #49 | "서울시 글로벌 SNS" → **"SNS 콘텐츠"** 리네임 |
| #50 | 계정 수집 버튼 facebook/instagram도 활성화(프론트 COLLECTABLE_PLATFORMS가 youtube만이었음) |
| #51 | **브랜드(client) 차원 활성화** + 대시보드 채널 식별(브랜드·언어·플랫폼·핸들). 기존 3계정 client='서울시' backfill |
| #52 | 콘텐츠 현황 페이지 — 계정별 주간 게재건수+월누적+합계+드릴다운 |
| #53 | 엑셀 내보내기 1단계 — 페이지별 .xlsx |
| #54 | 엑셀 내보내기 2단계 — 브랜드별 통합 워크북(월간요약+채널별콘텐츠), importer 왕복 호환 |
| #55 | SNS 콘텐츠 — 채널(플랫폼×어권) 셀렉터 제거, 계정 선택기로 일원화 |

설계: `docs/prd/DESIGN_UNIFIED_SNS_DASHBOARD.md`. 핵심 발견: 채널 = 클라이언트×언어×플랫폼. 무료수집(스크래핑)=0원/전직원, 유료(번역·요약)=Haiku 4.5(영속화로 재비용 방지).

## D. NAS 자료검색
| PR | 내용 |
|---|---|
| #56 | 검색 부서 필터를 Qdrant 코퍼스 기준 다중선택으로(RND 포함, #recycle 제외, 기본 전체). 폴더목록 출처 stale nas_files → Qdrant dept facet |

핵심: Qdrant dept = 마케팅(150만)·RND(6만)·경영지원팀(=COMPANY,2.2만)·신사업(1.3만)·마케팅본부(410). RND는 `/mnt/nas-rnd`에서 외부 파이프라인으로만 임베딩돼 nas_files엔 없었음.

## E. 검토 문서만 산출 (구현 보류)
- `docs/reviews/REVIEW_SECURITY_DOMAIN_CLEANUP_2026-06-22.md` — 보안(WireGuard·Cloudflare Tunnel·🔴open-webui 공개차단·SSH·디스크정리~50GB), 도메인
- `docs/reviews/REVIEW_INCREMENTAL_EMBEDDING_FRIDAY_SCHEDULE.md` — 금요일 18시 RunPod 임베딩(Qdrant 접근=푸시모델)

## 미해결 / 후속
- **보안 조치**: open-webui 공개차단·SSH 하드닝·WireGuard·Cloudflare Tunnel — 콘솔/계정 작업, 오너 결정 대기
- **디스크 정리 ~50GB**(docker prune 등) — 승인 대기
- **#6 임베딩 스케줄**: RunPod→Qdrant 접근방식 결정 필요
- **SNS 데이터 퀄리티**: 과거 게시물/메트릭 backfill, 수동 분류(구분: 행사/기획/정책) 입력 필드, 제작주체 집계, 릴스 캠페인 추적
- **엑셀 내보내기 (B)** NAS 폴더명 표기(top_folder 필드) — dept 대신 MARKETING/COMPANY/CLOUD/RND
- **보고서 양식** — 양식 받으면 작업
- docwork: PR-C(검수·렌더 공유화), form_filler/extractor를 documents/로 이전

## 오후/저녁 세션 — 작업방식 정립 + 전체 코드리뷰 + 보완

### F. 작업 방식(에이전트 팀) 정립
- **CLAUDE.md** 신설(루트, 세션 자동로드) — 기본 완전자율(구현→검증→머지→배포→검증→보고), 멈춰 묻는 건 파괴적/고비용 둘만, §7 **요청 라우팅**(만들어줘→feature-dev / 리뷰→멀티에이전트 / 퀄리티→simplify). **프로세스 변경은 같이 결정** 원칙.
- **docs/BACKLOG.md** 신설 — 할 일 모아두면 자율 처리. 승인 게이트: 아이디어→제안·계획→**오너 승인**→자율.
- **ECC 큐레이션(agent-sort, 멀티에이전트)** — off-stack 언어룰+zh를 `.claude/rules-library/`로 분리(매세션 노이즈 제거, DAILY룰=common/python/typescript/web). skill-library 라우터·훅끄기는 과해서 미적용.

### G. 전체 코드베이스 코드리뷰 (Workflow 오케스트레이션)
- **12개 모듈 병렬 리뷰+점수 → 종합 스코어카드** = `docs/reviews/CODE_REVIEW_FULL_2026-06-22.md`.
- **종합 등급 B−** (가중 6.3/10). 보안 5.75·테스트 1.63이 끌어내림, 유지보수 7.3 양호. 발견 94건(high 20·med 43·low 31, critical 0).
- 최대 리스크: NAS검색/RAG(C). 시스템적 이슈 9개(S-1 테스트부재 · S-2 RAG인가 · S-3 레이트리밋 · S-4 localStorage JWT · S-5 입력검증→500 · S-6 path · 등).
- ⚠️ 레이트리밋 교훈: 12개 동시 띄우면 API 순간폭주로 거절 → **3~8개씩 파도**로. 16코어=동시성 캡 14.

### H. 코드리뷰 보완 1차 — PR#59 배포
- 6 에이전트 병렬(파일소유 분리)→통합→검증→배포. 그룹1 + D 레이트리밋.
- 백: 로그인/LLM/생성 레이트리밋 · pool_pre_ping · 권한재동기화 · 입력검증(uuid/bcrypt/금액상한) · IntegrityError→409 · 토큰무효화 · path통일 · pptx차트가드 · 주차공식 floor · 테스트import · **E1 매칭불변식** · **D2 신사업팀 접근보존**(require_admin 제거).
- 프: drawer요약 영속 · 대시보드 key·5주차 · regex · 대량fetch cap · 디바운스 · 하드코딩URL.
- 검증 전부 green, 새 마이그레이션 0, 운영 확인.
- **보류(문서화)**: C localStorage→쿠키 · 테스트인프라(S-1) · Area A=전사공유 유지(결정) · G2 서버페이지네이션 · H3.

### 다음(최우선) = 코드 품질/아키텍처 전용 리뷰
보안/버그 리뷰와 다른 렌즈(설계·단순성·일관성·가독성·테스트용이성). BACKLOG NOW 최우선. 6~8개씩 동시.

## 운영 환경 메모
서버 Tencent CVM 서울(43.155.202.112), 배포=main 푸시→self-hosted runner `docker compose up -d --build`+`alembic upgrade head`. nginx `/api/` 타임아웃 300s. NAS RW=`/mnt/nas-rw`, RND=`/mnt/nas-rnd`. 검색=Qdrant `docs_text`(Qwen3 2560d) 단일소스, 외부 파이프라인 `tk101-rag` 적재. **작업방식: 루트 CLAUDE.md + docs/BACKLOG.md.**
