# BACKLOG — TK101

할 일을 여기에 적어두면 Claude가 우선순위 높은 것부터 **자율로 구현·배포·검증**한다(작업 방식: 루트 `CLAUDE.md`).
표시: `[ ]` 대기 · `[~]` 진행중 · `[x]` 완료(PR#) · `⚠️오너승인` = 시작 전 확인 필요(파괴적/고비용).

> 사용법: 그냥 아래에 한 줄씩 추가하면 됨. 큰 항목은 하위 `-`로 쪼개도 좋음. 우선순위는 위→아래.
> ✅ 완료된 건 **DONE 섹션**으로 내린다(여기 위쪽은 "남은 일"만 보이게).

---

## NOW (지금/다음 할 일 — 우선순위 위→아래)

> 오늘(6/23) 작업한 안전·자율 항목은 전부 완료·배포됨 → DONE 참조. 아래는 **남은 것**.

- [ ] ⚠️재정의필요 **엑셀 내보내기 (B)** — 항목 전제가 잘못 엮임(조사완료). "엑셀 내보내기"는 원래 SNS(PR#53/54)인데 dept/NAS폴더 없음. "부서→NAS폴더명"은 NAS 파일 얘긴데 NAS엔 엑셀 export 자체가 없음. **오너가 실제 니즈 확정 후 진행**: (a)SNS엑셀에 컬럼추가 (b)NAS검색결과에 폴더 표시/내보내기.
- [ ] ⚠️대형/논의 **TanStack Query 도입** — 서버상태 전면 교체. 전용 작업, 테스트 적어 신중. *(오너 보류 중)*
- [ ] ⚠️영향큼 **API `res.data` 언랩 컨벤션 통일** — 모든 호출부 영향. 신중. *(오너 보류 중)*
- [ ] ⚠️논의 **localStorage JWT → httpOnly 쿠키** — 인증 저장모델, 로그인 흐름 위험. 논의부터. *(오너 보류 중)*
- [ ] 보류·선택 **양식 단일컴포넌트 병합** — 페이지 reload 제거. Steps바+서버영속으로 wayfinding/데이터보존은 이미 해결 → 순수 미관, 4페이지 전면 리팩토링이라 회귀위험 큼. 실사용 후 정말 필요하면.

## 계획 수립됨 (2026-06-30, docs/prd/PLAN_*.md — 착수 대기)
- [ ] **동아제약 운영보고서 자동작성** — 구글시트 데이터 → 기존 양식 자동 채움(form_filler). P1(수동업로드)부터. → `docs/prd/PLAN_DONGA_OPS_REPORT_2026-06-30.md`. *(위 "보고서 생성" 항목의 구체화 — 오너 양식파일+시트샘플 받으면 착수)*
- [ ] **NAS 이미지 임베딩** — VLM 캡션→임베딩, P0 센서스+P1 파일럿부터. P2 대량은 ⚠️비용승인. → `docs/prd/PLAN_NAS_IMAGE_EMBED_2026-06-30.md`
- [ ] ⏸️보류 **NAS 증분 임베딩** — 신규/변경/삭제 동기화(현재 increment 전무). 계획서만, 구현 보류(오너). → `docs/prd/PLAN_NAS_INCREMENTAL_EMBED_2026-06-30.md`

## 제품 기능 (안전·자율, 조만간)
- [ ] **SNS 데이터 퀄리티** — 과거 게시물/메트릭 backfill(메타·유튜브 API 깊이 긁기), 누락 메트릭 보정
- [ ] **릴스 캠페인 추적** — 차수/EP/주제 + 크로스플랫폼(웨이보·페북) 성과 비교 (시트의 릴스 시리즈 탭)
- [ ] **보고서 생성** — 항목별 보고서 양식 맞춤 (⚠️ 오너가 양식 전달 후 착수) → 동아제약 건으로 계획 구체화(위 참조)

## 코드 품질 후속 (전체 코드리뷰 2026-06-22 → docs/reviews/CODE_REVIEW_FULL_2026-06-22.md)
- [ ] **S-1: 테스트 인프라 구축** — 인증·금액·매칭 핵심 로직 pytest/vitest 안전망(현재 8개뿐). 다른 보완의 전제, 별도 큰 작업
- [ ] **G2 후속**: ProductsPage 서버사이드 페이지네이션(현재 cap+경고만) · MappingTable 부모 PATCH+refetch(키스토크 디바운스는 완료)
- [ ] **H3 후속**: `UserCreate.password` min/max_length (schemas/user.py)
- [ ] **결정기록**: NAS검색 = 전사 공유 유지(부서 스코핑 OFF 의도적). confidential 필터·by-path 다운로드 인가는 추후.
- [ ] 나머지 medium/low (토큰무효화 J·세션확장성 K 등) — 리뷰 문서 부록, 우선순위 낮음

## NAS 검색 품질 — 재임베딩 후속 (⚠️비용·오너승인)
- [ ] ⚠️비용 **H4: 파일명·고객사·연도를 청크 텍스트에 prepend** — 본문 약한 정답 점수↑(ROI 1위). *P2 path인덱싱으로 파일명검색은 일부 무료 해소됨.*
- [ ] ⚠️비용 **C2: 정크 청크 제거** — URL인코딩·빈 구분자·헤더없는 숫자행. 인덱스 축소→재임베딩 비용·노이즈 동시↓.
- [ ] ⚠️비용 **H3: 엑셀 행에 헤더 prepend / 문서 구조 보존** — `12 | 서울 | 3500` 류 의미 약함.
- [ ] (선택) **검색 레이턴시** — 대부분 리랭크 풀20(~7~8s). 더 줄이려면 `nas_rerank_top_n` 16으로 절충.

## ⚠️ 오너 승인/입력 필요 (시작 전 확인 — 콘솔/계정/파괴/고비용)
- [ ] ⚠️오너승인 **보안 조치** — open-webui :3000 공개차단, SSH 하드닝, backend-dev 8001 바인딩, WireGuard 키, Cloudflare Tunnel. → `docs/reviews/REVIEW_SECURITY_DOMAIN_CLEANUP_2026-06-22.md`
- [ ] ⚠️오너승인 **디스크 정리 ~50GB** — `docker image/builder prune` + tk101-dev-nas(stale) 제거. (파괴적)
- [ ] ⚠️오너승인 **금요일 18시 임베딩 스케줄** — RunPod 서버리스 + Qdrant 접근방식 결정. → `docs/reviews/REVIEW_INCREMENTAL_EMBEDDING_FRIDAY_SCHEDULE.md`

---

## DONE (최근)

**2026-06-23 (오늘):**
- [x] **문서작업 품질·UX 개선 전체** — Phase1 temperature·grounding·docgen16K(#77) · Phase2 bridge리랭커(#78)·양식 좌표앵커 표셀채움(#80)·docgen 스켈레톤+judge루프(#79) · Phase3 UX 에러·진행률·필터·텔레메트리(#82)·위저드Steps(#84)·실제 docx미리보기 mammoth(#86)·작성중문서 resume(#88). 의도적 보류=단일컴포넌트 병합.
- [x] **NAS 검색 품질** — 게이트 리랭크後 이동+풀12→20(#75) · **풀텍스트 인덱스 P2**(#92, 정확검색 전체 코퍼스 복원: '견적서' 30,123 vs 과거 100건).
- [x] **SNS 구분 필드**(#91, 마이그035) · **제작주체 집계**(#98, GROUP BY 동적) · **docwork PR-C**(#97, extractor 이전) · **포매터/린터 셋업**(#101, prettier+ruff·black).

**2026-06-22~23:**
- [x] **코드리뷰·리메디에이션 사이클** — 보안B−+품질B− 전수리뷰, 보완 6차례(PR#59), 거대파일 10개 분할, 커버리지 20건. → worklog 2026-06-23.md
- [x] NAS 검색 부서 필터(#56) · SNS 채널셀렉터 정리(#55) · 엑셀 내보내기 1·2단계(#53/54) · 콘텐츠 현황(#52) · 브랜드 활성화(#51) · SNS 통합·권한·버그픽스(#39~50) · 문서작업 통합(#31~43) · 레거시 pgvector 제거(#32/38)

> 전체 이력: `docs/worklogs/2026-06-22.md`, `2026-06-23.md`
