"""SQLAlchemy ORM 모델.

Sprint 1 domain-db scope에서 추가 예정:
- user_model.py (users 테이블)
- department_model.py (departments 테이블)
- chat_message_model.py (chat_messages 테이블)
- refresh_token_model.py (refresh_tokens 테이블)

주의: ORM 모델은 Infrastructure 레이어. Domain Entity와 분리.
Repository가 ORM 모델 ↔ Domain Entity 변환 담당.
"""
